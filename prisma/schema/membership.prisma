model Membership {
  id              String   @id @default(uuid())
  name            String   // e.g. "Basic", "Premium"
  price           Decimal
  durationInDays  Int
  maxBorrowLimit  Int
  subscriptions   Subscription[]
}